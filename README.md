#ASP_API_Angular_BasicMessageBoard

Backend: ASP.NET Core API
Front: Angular2

Very basic website that takes in a message. With a specific name.

Error handling for when server is down as well as when messages cannot be sent.
